1. Создать новый репозиторий в github с названием git_intro
2. Создать папку и инициализируйте новый репозиторий у Вас на компьютере с таким же именем.
3. Скопировать любую картинку или фото в эту папку. 
4. Отправить эти изменения в репозиторий git_intro  в github.  
Для этого после копирования фото сделайте:
git init
git add ИМЯ ФАЙЛА
git commit -m "Added photo"
git branch -M main
git remote add origin git@github.com:ВАШЕ ИМЯ НА GITHUB/git_intro.git
git push -u origin main
5. Прислать ссылку на Ваш репозиторий  git_intro с выполненными действиями.

1. Create a new repository in GitHub named git_intro
2. Create a folder and initialize a new repository on your computer with the same name.
3. Copy any image or photo to this folder.
4. Push these changes to the git_intro repository on GitHub. 
To do this, after copying the photo, do the following:
git init
git add FILE NAME
git commit -m "Added photo"
git branch -M main
git remote add origin git@github.com:YOUR GITHUB NAME/git_intro.git
git push -u origin main
5. Send a link to your git_intro repository with the completed steps.