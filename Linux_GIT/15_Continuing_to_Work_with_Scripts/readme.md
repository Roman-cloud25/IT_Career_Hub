Purpose sleeper.sh
The script is designed to create a set of files with numeric names, 
and also contains commented commands for collecting system information.

Displays the time and number of processes every 5 seconds (in a 10-loop)
Saves CPU information to `cpu_info.txt`
Saves the operating system name to `os_info.txt`