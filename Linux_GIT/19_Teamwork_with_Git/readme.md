Purpose files_splitter.sh
The script creates 100 files with random numerical names and distributes them among folders:

- checks that the name consists only of numbers.
- `odd` is the initial directory where all files are initially placed.
- `even` is the directory where files with even names are moved.